# Representative excerpt

One file, included solely to evidence original expression in the source code:
the frozen data-contract types that fix the application's domain model — users,
roles, driver client preferences, and the shapes every screen consumes.

This is a fragment of the type definitions only. It is not a functioning system.
The pricing engine, matching and negotiation logic, authentication, and all
server code are deliberately excluded and remain private, as
[`../README.md` §6](../README.md#6-what-is-deliberately-not-here) explains.

Nothing here contains credentials, endpoints, rate cards or personal data.
